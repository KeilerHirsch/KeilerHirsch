name: Profile activity

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - README.md
      - tools/render_activity_panel.py
      - tests/**
      - .github/workflows/profile-signal.yml
  pull_request:
    paths:
      - README.md
      - tools/render_activity_panel.py
      - tests/**
      - assets/activity-panel.svg
      - .github/workflows/profile-signal.yml

permissions:
  contents: write

concurrency:
  group: profile-activity-${{ github.ref }}
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true

      - name: Verify profile surface
        run: python -m unittest discover -s tests -v

      - name: Render activity panel
        id: activity
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          python tools/render_activity_panel.py --output assets/activity-panel.svg
          if git diff --quiet -- assets/activity-panel.svg; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit changed activity panel
        if: steps.activity.outputs.changed == 'true' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- assets/activity-panel.svg
          git commit -m "chore: refresh profile activity"
          git push
